/**
 * Venue Model
 * Schema: id, camera_id, name, status, frame_width, frame_height, grid_rows, grid_cols, created_by, created_at, updated_at, last_metadata_time
 * Indexes: camera_id (unique), created_by
 */

// TODO: Venue model schema and methods
