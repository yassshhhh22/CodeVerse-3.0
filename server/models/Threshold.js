/**
 * Threshold Model
 * Schema: id, venue_id, zone_id (nullable), warning_level, critical_level, created_at, updated_at
 * Indexes: venue_id, zone_id
 */

// TODO: Threshold model schema and methods
