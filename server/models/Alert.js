/**
 * Alert Model
 * Schema: id, venue_id, zone_id (nullable), severity, density_value, message, triggered_at, acknowledged_by (nullable), acknowledged_at (nullable)
 * Indexes: venue_id, zone_id, triggered_at
 * TTL: Auto-delete after 90 days
 */

// TODO: Alert model schema and methods
