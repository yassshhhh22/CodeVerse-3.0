/**
 * CV Metadata Handler
 * Receives metadata from Python CV system via WebSocket
 * Orchestrates: gridDensity → zoneMapping → aggregation → threshold check → alerts
 * Event: "new_metadata" from Python CV
 */

// TODO: CV metadata handling logic
