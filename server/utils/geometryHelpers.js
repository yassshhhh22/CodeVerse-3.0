/**
 * Geometry Helpers
 * isPointInGridCell(x, y, cellX, cellY, cellWidth, cellHeight)
 * getGridCell(x, y, frameWidth, frameHeight, gridRows, gridCols)
 * isPointInZone(x, y, zone) - checks if point is in zone's grid cell range
 */

// TODO: Geometry utility functions
